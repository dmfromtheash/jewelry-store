# Safe placeholders only. Do not put real secrets in this file.
APP_NAME="Jewelry Store"
APP_ENV="local"
APP_URL="http://localhost"

# Real payment, auth, database, and production integration values are not configured during bootstrap.
